<div class="container">
  <div class="row justify-content-center">
    <div class="col-6">
      <%= simple_form_for [@plant, @completion] do |f| %>
        <%= f.association :chore %>
        <%= f.submit "Ajouter une tâche réalisée" %>
      <% end %>
    </div>
  </div>
</div>
